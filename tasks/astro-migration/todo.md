# Task List — Astro 7 / Keystatic / Cloudflare Migration

**Status:** Checkpoint 3 approved (2026-08-07); Tasks 4.1-4.2 complete, Task 4.3 active; `/link` Plasma restyle human-approved and locked 2026-08-08 (Task 3.6 amendment); Music section shipped an animated shader backdrop 2026-08-08 (Task 4.3 amendment); Hero reworked twice more on human direction (Task 4.2c, then Task 4.2d 2026-08-09 — plate restored over the keyed cutout, one shoot at both breakpoints, CSS-sticky push into Music) and now has a one-time, cancellable Lenis auto-handoff into Music, all **awaiting human visual approval**; the Hero→Music seam that Task 4.3 left open is now built from both sides; Checkpoint 3's approved capture set depicts the superseded hero; Task 2.4 open only on the final domain and not gating
**Latest Task 4.3 amendment (2026-08-17):** the Hero now replays a cancellable Lenis auto-intro on every normal page load, stops on frame 117 at the frame-scrub endpoint, and leaves Music one viewport below for manual scrolling. It remains awaiting human visual approval.
**Current production:** Keep Next.js + Supabase + Vercel live until Checkpoint 7

## Blockers and decisions

- [x] Project-work scope: work only in this `Kev.Sun-astro-7-cloudflare` migration worktree. `Kev.Sun` is the legacy build and must not be changed. Remain local and do not use GitHub for this project unless the human explicitly changes that direction.
- [ ] Supply the final public domain. This blocks Task 2.4: `astro.config.mjs` has no `site` value, so the canonical, `og:url`, and `twitter:image` branches in `BaseLayout.astro` emit nothing. One-line fix once supplied.
- [ ] Decide CMS timing: keep Keystatic at Task 5.1 (default) or bring it up in local mode against the existing collections right after Checkpoint 2. The earlier option is a reorder and needs explicit approval.
- [ ] Confirm the intended Keystatic editor and GitHub write access for `gesproject/kev-test-build`.
- [ ] Confirm Cloudflare account/project ownership and domain/DNS access before Phase 7.
- [ ] Confirm whether content editors may have GitHub write access or require Keystatic Cloud.
- [ ] Confirm font conversion/subsetting rights before shipping a webfont. The legacy Neue Haas source files remain in the separate `Kev.Sun` repository; by human direction (2026-08-05), this migration uses system fallbacks until the web-embedding implementation and rights decision are supplied.
- [ ] Supply final Spotify, SoundCloud, ticket, booking, and social URLs where placeholders remain. The `/link` LISTEN and BUY actions currently have no destination and render as non-interactive `LINK PENDING` cards; social and booking destinations are real.

## Phase 0 — Baseline and parity contract

- [x] Task 0.1: Freeze the recoverable baseline; legacy Supabase content migration waived by human approval (2026-07-22). Evidence: baseline commit/prod candidate and successful build recorded; Vercel/Supabase unchanged and recoverable.
- [x] Task 0.2: Capture and approve mobile/tablet/desktop visual and interaction references. Evidence: human approved the recorded contract on 2026-07-25; see `tasks/astro-migration/visual-interaction-contract.md` and `references/task-0.2-production/`. Sparse Music/pin evidence and documented motion/accessibility gaps remain approved parity exceptions.
- [x] Task 0.3: Record reproducible network, bundle, Lighthouse, and hero-loading baselines. Evidence: verified local production-build record, cold mobile 193-frame/19.62 MB waterfall, and three Lighthouse reports; budgets remain unamended.

### Checkpoint 0: Baseline and parity contract approved

Approved explicitly by the human on 2026-07-27. The dedicated `/link` preview and its two research inputs are now approved migration scope.

## Phase 1 — Risk spikes

- [x] Task 1.1: Deploy the pinned Astro/Cloudflare compatibility spike. Evidence: exact compatibility matrix, local workerd checks, and public static/dynamic Cloudflare preview verification are recorded in `tasks/astro-migration/task-1.1-compatibility-spike.md`; the no-route Worker is isolated from production traffic.
- [x] Evaluation preview E1: User-approved static `/link` poster in an isolated no-route Astro preview before Task 1.2. It is a visual/product test only, uses the Sölbo material system and real local assets, ships no public React or Hero sequence, and does not satisfy or reorder the production Task 3.6 acceptance. **Approved by the human on 2026-08-03.** Direction: follow + site-funnel as twin goals, action-first mobile fold, concrete/paper editorial retained, furniture trimmed (jump-nav, duplicate wordmark, standalone contact panel removed), and a compact subtle cinematic-site CTA. Evidence: `references/link-preview/src/pages/link.astro`, revised 375×812/768×1024/1440×900 captures in `references/link-preview-evidence/`; successful isolated build, 0 script/video/preload tags, and 74.9 KiB full gzip against the 200 KiB budget.
- [x] Task 1.2: Keystatic GitHub auth, CRUD, preview, deploy, failure, and rollback proof completed against canonical `gesproject/kevsunastro`. Test `main` holds reviewed proof configuration at `53d495a`; editor commit `7aef870` on `keystatic-proof/bootstrap` produced version `3785deac` in about 78 seconds without changing active deployment. Branch-only intentional failure `5a3f985` failed with no uploaded version; authorized revert `ec91cdb` passed local `npm run check` and recovered preview version `f817939b`. The older `kev-test-build` Worker was restored to `cd9a3905` and disconnected from Builds; its subsequent version history was unchanged. The Next.js/Vercel/Supabase production path was untouched. Evidence: `task-1.2-keystatic-github-proof.md`.
- [x] Task 1.3: Benchmark progressive sequence vs scrub-capable video for the hero. Evidence: seven variants measured twice for bytes, requests, decode/seek, cache, working-set memory, and SSIM in `tasks/astro-migration/task-1.3-hero-delivery-benchmark.md` with raw data in `references/task-1.3/results/`; H.264 CRF23 GOP10 (3.04 MiB, 38% of the 8 MiB budget) is recommended with no budget exception, first interaction verified at 4.1% of the media downloaded, and poster fallback verified not to trap scroll. Real-device iOS scrub and low-memory behavior remain unmeasured and are flagged for Task 1.4.
- [x] Task 1.4: Architecture decision recorded and human-approved at Checkpoint 1 on 2026-08-05: Astro 7 + Cloudflare, Keystatic GitHub mode, static assets, AVIF Hero delivery, `/link`, zero public React, and no Markdoc. The approved source mapping is wide master → desktop Hero; close companion → mobile Hero. H.264 seeking was janky on iPhone 17 Pro Max Safari, so Task 4.2 uses the documented AVIF-sequence fallback; no flash, pause, fullscreen, sound, or scroll trap was observed. Evidence: `task-1.4-architecture-decision.md` and `references/task-1.4-hero-delivery-preview/`.

### Checkpoint 1: Architecture and hero delivery approved

Explicitly approved by the human on 2026-08-05. Task 2.1 is authorized.

## Phase 2 — Foundation

- [x] Task 2.1: Create the migration worktree and pinned Astro foundation; fix stale agent rules. Evidence: the root app now has the pinned Astro 7/Cloudflare foundation, deterministic Node/npm and lockfile pins, migration-aware `AGENTS.md`, and `npm run check` / `npm run build` pass locally with the generated Cloudflare Worker artifact. `npm run dev` serves the foundation locally at `/`; no GitHub, Cloudflare deployment, DNS, Vercel, or Supabase mutation occurred.
- [x] Task 2.2: Add vanilla CSS tokens, base styles, and accessibility foundations. The CSS uses system fallbacks; importing or converting the legacy Neue Haas files is explicitly deferred pending the human's later web-embed implementation and licensing confirmation.
- [x] Task 2.3: Add validated shows/releases/links collections and the site singleton. Astro Content Layer validates curated JSON at build time; the homepage consumes the singleton and deterministically sorted link actions without a browser fetch. A temporary invalid-link `astro sync` check was rejected as expected and removed. No stale mock records or Hero assets were copied; verified shows/releases are intentionally deferred to Task 5.2. Human clarification (2026-08-05): Keystatic's transitive `@markdoc/markdoc` package is allowed, but authored Markdoc/rich-text content remains forbidden without a separate approval.
- [ ] Task 2.4: Add base layout, metadata, social tags, favicon, and semantic shell. `src/layouts/BaseLayout.astro` and `public/favicon.svg` are in place with the semantic shell, skip link, title/description, and Open Graph/Twitter tags. Remaining work is only the absolute canonical/social URLs, which stay inert until `site` is set in `astro.config.mjs` from the supplied domain.

### Checkpoint 2: Foundation reviewed

Explicitly approved by the human on 2026-08-06. Task 3.1 is authorized. Task 2.4 remains open only on the final public domain; it does not gate Phase 3.

## Phase 3 — Static section port

- [x] Task 3.1: Port Music markup and vanilla CSS. Implementation and machine verification are complete: `src/components/Music.astro` ports the players, fallbacks, release grid, platform links, background layers, and the 768px layout switch with zero client JavaScript; `npm run check` (0/0/0), `npm run build`, and `npm test` (2/2) pass on the pinned Node 22.12.0, and the prerendered route emits **0 script tags**. Motion stays in Task 4.3 by design. Evidence and the recorded parity exceptions: `task-3.1-music-port.md`. Review found and closed a stored-XSS path: `z.url()` validated URL shape but not scheme, so a CMS-authored `javascript:` or `data:` href would have executed in-origin; `schemas.ts` now allowlists schemes for every URL field. Viewport captures and keyboard-focus pass completed 2026-08-07 via Playwright/wrangler dev — see `references/checkpoint-3-captures/` and the Checkpoint 3 note below.
- [x] Task 3.2: Port Shows markup and vanilla CSS. `src/components/Shows.astro` ports the header, list (date/venue/city, ticket-link/sold-out/free states), and the desktop-only photo panel with zero client JavaScript; renders the list once with a media query instead of legacy's duplicate-DOM per-breakpoint render. `npm run check` (10 files, 0/0/0 + 3/3 tests) and `npm run build` pass; prerendered route emits 0 script tags. Review found and fixed an accessibility regression (header labels were `aria-hidden`) and a dead-code font-size bug of the same shape as a resting-opacity mistake caught during implementation. Evidence: `task-3.2-shows-port.md`. Viewport captures and keyboard-focus pass completed 2026-08-07 — see `references/checkpoint-3-captures/`.
- [x] Task 3.3: Port Footer and wave containers. `src/components/Footer.astro` ports the booking block, content-driven link row + copyright, and both watermark layers with zero client JavaScript; the wave field itself is deliberately left as an empty host div since Task 4.4 hasn't chosen a render strategy yet. `npm run check` (11 files, 0/0/0 + 3/3 tests) and `npm run build` pass; 0 script tags emitted. Review found and fixed a desktop spacing regression (flat 64px vs legacy's 220px) and a box-model bug in the watermark's negative-margin layout that the implementation phase's own earlier fix had reintroduced. `getPlatformLinks()` extracted to `src/lib/content.ts`, now shared with Music.astro. Evidence: `task-3.3-footer-port.md`. Viewport captures and keyboard-focus pass completed 2026-08-07 — see `references/checkpoint-3-captures/`.
- [x] Task 3.4: Port Hero static states and fallbacks. `src/components/Hero.astro` + `src/components/HeroSocialNav.astro` port the headline, enter affordance, poster/canvas shell, loading fallback, and responsive overlays with zero client JavaScript; `lucide-react` icons and the `motion/react` wave loader are replaced with inline SVG and CSS animation. Structural departure from legacy: the mobile intro and desktop lockup are two independent CSS-toggled compositions (each with its own `<h1>`) rather than a JS-gated cover, since the JS-gated version would leave real content permanently inaccessible without JavaScript — a direct Phase 3 acceptance-bar conflict. `npm run check` (13 files, 0/0/0 + 3/3 tests) and `npm run build` pass; 0 script tags emitted. Review found and fixed a Critical bug (loading indicator ignored its own `hidden` attribute — an author `display:flex` rule always beats the UA `[hidden]` rule) and an Important one (desktop canvas mask would pin to the wrong edge once Task 4.2 fills it, from an over-constrained `inset`+`width` combination). Evidence: `task-3.4-hero-port.md`. Viewport captures and keyboard-focus pass completed 2026-08-07 — see `references/checkpoint-3-captures/`.
- [x] Task 3.5: Integrate page order, overlaps, seams, and responsive parity. Page order (Hero → Music → Shows → Footer) was already composed landing Task 3.4; this task's audit traced every section-boundary background color from source (not assumption) at both breakpoints and found all three seams already continuous on the same shared tokens. Also confirmed: unique ids, correct h1→h1→h2→h2 heading hierarchy, no landmark or z-index/position conflicts, no stray `position:fixed`, reduced-motion coverage centralized and complete. No code changes — nothing found needed a fix. Evidence: `task-3.5-integration.md`. Pixel-level review against the composed page across all three reference viewports completed 2026-08-07 (see `references/checkpoint-3-captures/`) — DOM order, seam colors, and spacing all matched the source-level audit with nothing further to fix.
- [x] Task 3.6: Build and preview the dedicated `/link` social landing route within its 200 KiB/no-React/no-Hero-preload budget. `src/pages/link.astro` wires the human-approved E1 visual system (approved 2026-08-03) to real build-time content, importing nothing from the cinematic homepage's component tree so it can't pull in Hero media, motion libraries, or React. `npm run check` (14 files, 0/0/0 + 3/3 tests) and `npm run build` pass; 0 script tags emitted; measured transfer ≈7.7 KiB gzip-equivalent against the 200 KiB budget. Review found and fixed a duplicate skip-link (route double-rendered `BaseLayout`'s own), disabled social links silently vanishing instead of showing the pending state every other content type on the page already gets, and a release-card CSS grid that would collapse without artwork; plus an `Object.hasOwn` hardening on the social-icon lookup. Evidence: `task-3.6-link-route.md`. Viewport captures and keyboard-focus pass completed 2026-08-07 — see `references/checkpoint-3-captures/`. Delivered preview URL (human-approved deploy, 2026-08-07): `https://9b3b6305-solbo-astro7-cloudflare.nickgagne92.workers.dev/link/` — no route/domain attached, isolated from production.

### Checkpoint 3: Static visual parity approved

**Approved explicitly by the human on 2026-08-07. Task 4.1 is authorized.**

Playwright + `wrangler dev` (against the real built Worker, per `playwright.config.ts`) closed the browser gap that blocked Tasks 3.1–3.6 all prior session. Evidence: `references/checkpoint-3-captures/` — 375×812/768×1024/1440×900 fullPage captures of `/` and `/link`, a reduced-motion pass at 1440×900, and a keyboard tab-order log with focus-visible-outline check per stop (11 stops on `/`, 7 on `/link`, all with a visible outline and an accessible name). Capture script: `e2e/checkpoint-3-evidence.spec.ts`.

Findings from the review, both closed:
- One capture-tooling artifact, not a site bug: Playwright's `fullPage` screenshot doesn't always paint far off-screen `position: sticky` content (Shows' desktop photo panel) before capturing. Fixed in the capture script itself (force-eager images, scroll the page top-to-bottom before shooting) — confirmed by isolated repro that the live page renders the photo correctly at every breakpoint.
- The keyboard-focus script's own loop-detection first false-positived (Footer's link row shares text/href with Music's, which isn't a DOM identity), truncating the tab order at 8 stops instead of the real 11. Fixed by tagging visited DOM nodes instead of matching on content; corrected order confirms every element, including the two icon-only Hero social links, has a real accessible name (`aria-label`) and a visible focus ring.

No visual-parity defect found against the approved baselines (`references/task-0.2-production/`, `references/link-preview-evidence/`). Human reviewed the live preview in-browser (Task 3.6 URLs below) before approving.

Human decisions made at this checkpoint (2026-08-07):
- **WCAG AA contrast on legacy-inherited text (platform links ≈2.3:1, pending labels ≈1.8:1, release meta ≈2.25:1): keep as-is.** Confirmed as an accepted parity exception, consistent with the Checkpoint 0 approved exceptions. No visual change.
- Task 3.6's delivered preview URL: **done 2026-08-07**, human-approved deploy. `solbo-astro7-cloudflare` had no prior deployment, so this took an initial `wrangler deploy` (also separately human-approved) rather than Task 1.2's `versions upload`. No route or custom domain is attached (`workers_dev: false`, no `routes` in `wrangler.jsonc`), isolated from production the same way Task 1.1's spike Worker was. Live: `https://9b3b6305-solbo-astro7-cloudflare.nickgagne92.workers.dev/` and `.../link/`, both HTTP 200.

Gate still carried forward, not blocking: Task 3.1 leaves a `ponytail:` marker in `Music.astro` — once Task 5.2 supplies real release URLs, the Spotify/SoundCloud iframes replace the static cards and render as empty boxes with JavaScript disabled. Unreachable today (both collections are empty); close it when Task 5.2 lands.

## Phase 4 — Motion and media

- [x] Task 4.1: Add scoped Lenis/GSAP/ScrollTrigger lifecycle. `src/lib/motion/lifecycle.ts` is the shared engine for `/` only: one Lenis instance driven by `gsap.ticker`, `ScrollTrigger` registered and synced via `lenis.on("scroll", ScrollTrigger.update)`, matching the legacy `SmoothScroll.tsx` wiring exactly. It adds two things the legacy React version didn't need: an idempotent guard (`data-motion-lifecycle` on `<html>`, also a test hook) so re-entry is a no-op, and a `pagehide`/`pageshow` pair so a bfcache-restored page (back button) tears down and reinitializes cleanly instead of coming back with a dead ticker. Skips entirely under `prefers-reduced-motion: reduce`, leaving native scroll untouched. `src/pages/index.astro` is the only route that loads it; `src/pages/link.astro` is untouched. `npm run check` (15 files, 0/0/0 + 3/3 tests) and `npm run build` pass; built output confirms exactly one script tag on `/` (no React markers in the bundle — verified by direct inspection, not inferred) and zero on `/link`. Task 4.1 is the first task in this migration to ship any client JavaScript. Evidence: `e2e/task-4.1-motion-lifecycle.spec.ts` (4 assertions: single clean activation, idempotent guard against a synthetic bfcache-restore signal, reduced-motion skip with native scroll still functional, `/link` isolation, and a real back/forward cycle), all passing against the real built Worker via `wrangler dev`. Review (`agent-skills:code-reviewer`) approved with no correctness issues; traced Lenis's `destroy()` and gsap's `ScrollTrigger` internals directly against the installed package versions (gsap 3.15.0, lenis 1.3.26) rather than assuming API shape. `gsap`/`lenis` added as exact-pinned dependencies, matching this repo's existing pin convention.
- [x] Task 4.2: Implement the progressive, failure-safe hero sequence. `src/lib/motion/heroSequence.ts` is the engine: progressive per-breakpoint frame loading (never blocks entry), DPR-capped cover-fit canvas draw, nearest-loaded-frame fallback, mobile pre-entry lock + "See website" enter behavior, ScrollTrigger scrub per breakpoint, a reduced-motion static path (loads only the final frame, never the sequence), and its own bfcache guard/teardown/reinit mirroring Task 4.1's `lifecycle.ts` pattern. Two new real AVIF sequences (193 mobile frames at native 1176×1080, 233 desktop frames at 1920×1080) replace the empty canvas shell, encoded with Task 1.3's approved crf32 settings from two new source videos supplied this session. Found and removed in passing: `public/frames-mobile/` (the old 193-JPEG sequence, 233% over the mobile hero budget, explicitly rejected by Task 1.4) was still tracked and still shipping in every build. `npm run check` (16 files, 0/0/0 + 3/3 tests) and `npm run build` pass; `/` ships exactly one script tag (51.7 KiB gzip, no React markers) and `/link` stays at zero. Two real bugs found and fixed during implementation: the mobile scroll lock's wheel/touch prevention didn't work once Lenis is active (Lenis applies scroll via its own `scrollTo()` regardless of other listeners' `preventDefault()` — fixed by routing through Lenis's own `stop()`/`start()`, added to `lifecycle.ts` as `pauseMotion()`/`resumeMotion()`); and `initHeroSequence()` had no bfcache reinit of its own, unlike Task 4.1's lifecycle module. Independent review (`agent-skills:code-reviewer`) found one more, more serious bug — the mobile frame-completion math was a GSAP timeline-duration trap that made frames finish at 100% of scroll instead of the intended 64%, verified and fixed by deriving frame index directly from `ScrollTrigger`'s own progress instead of a timeline+tween; post-fix, "See website" now lands pixel-identical (0/304,500 differing pixels) to the true final frame. Confirmed, not a code defect: the desktop crop shows no visible motion for this specific footage (0% pixel change across the whole sequence in that exact crop region, vs. 9% across the full uncropped frame) — a framing question flagged for human review, not something fixed here. Evidence: `task-4.2-hero-sequence.md`, `e2e/task-4.2-hero-sequence.spec.ts` (6 tests). Full regression suite 22/22 passing.
- [x] Task 3.6 amendment — `/link` Plasma restyle (human-directed, approved and locked 2026-08-08). Reopens the Checkpoint 3-approved `/link` visual system at the human's request: dark ground, large portrait hero, single narrow centred column over an animated WebGL "Plasma" backdrop, flat monochrome social row. Acceptance criteria, budget, and isolation rules are unchanged and all re-verified — 85.9 KiB of a 200 KiB budget, zero public React, zero Hero-sequence requests, full no-JavaScript path, reduced motion renders the artwork frozen (0 rAF calls), hidden tab pauses the loop, DPR capped at 2. **This route now ships exactly one script tag** — the decorative backdrop, the first client JavaScript `/link` has ever carried — so `task-4.1-motion-lifecycle.spec.ts`'s "0 script tags" assertion was rewritten to test what it was protecting (no motion library, no React, no Hero sequence) rather than a count of zero. Resolved a contradiction in the supplied brief: it specified both a cyan palette and `hue 174°`, which the shader applies as a post effect, rendering that palette copper; the human chose to keep the specced copper, so every foreground colour was re-derived through the shader's own YIQ rotation instead of by eye. Contrast was measured against the brightest pixel the shader actually paints, which caught a defect this restyle introduced — the accent focus ring at 2.3:1, under the 3:1 minimum — fixed by re-ramping the veil and giving on-backdrop small type its own alphas; all 11 pairs now pass, an improvement on Checkpoint 3's accepted parity exception. New asset `public/images/solbo-portrait.webp` (27.3 KiB) derived from untracked Phase 4 source media. Evidence: `task-3.6-link-plasma-restyle.md`, `references/link-plasma-restyle/`, `e2e/link-plasma-backdrop.spec.ts` (5 tests). Full regression suite 27/27 passing. Still open by choice: LISTEN/BUY remain "SOON" pending real destinations (Task 5.2); three named micro-cuts (−21 lines) left unapplied.
- [x] Task 4.2 amendment — hero restyle: keyed footage over a shader backdrop, socials removed (human-directed 2026-08-08, pending human visual approval). Closes Task 4.2's open "desktop crop framing" item, which turned out to be more than framing: the desktop canvas is confined to a strip on the right of the viewport while `coverFit` centred the image inside it, so the visible window mapped to frame x 33%–67% — the middle of the plate — and **the artist was never on screen on desktop at all**; the scrub was animating a static grey rectangle, which is why that region measured 0.000% pixel change. Fixed with `anchorX: 1`. The desktop shoot's background measured a mathematically flat rgb(187,187,187) across all 233 frames (one distinct colour, zero deviation), so it is now baked to alpha at asset-prep time and the supplied 21st.dev "Simplex Noise" shader shows through behind the artist — `src/components/HeroBackdrop.astro`, built on the same plain-WebGL1 Astro pattern as Task 3.6's `PlasmaBackdrop.astro`, no React, no Tailwind, no shadcn (why: `task-4.2b-hero-keyed-backdrop.md`). The key needed real care, not a default: the nearest subject pixel sits only 21 from the plate, and a first pass at `colorkey` similarity 0.04 punched holes through his blonde hair, ear and cheek — re-cut at 0.012/0 and verified numerically (0 subject holes, 0 plate left opaque, across 7 frames spanning the sequence) rather than by eye. WebP rather than AVIF because this ffmpeg build cannot write an AVIF alpha plane (four routes tried; libaom rejects the gray alpha stream). Desktop sequence 2.5 MB → 3.3 MB, and cropping to the measured subject union box cuts decoded bytes per frame 4.4×, which matters more than transfer since the sequence is held decoded. Mobile is deliberately untouched — a different shoot with a real room behind the artist (mean deviation 36), not keyable and not needed, so `.hero-backdrop` never renders below 768px. Social nav removed from both hero compositions and `HeroSocialNav.astro` deleted; `Music.astro`/`Footer.astro` still carry the full list, so this is a hero-scope removal, but it **does contradict the human-approved `visual-interaction-contract.md`** ("Hero contains ... social navigation") and drops `/` from 11 keyboard focus stops to 8 — flagged, contract file deliberately not edited. Three fixes fell out of the keying: the desktop bottom vignette was fading the *artist* to near-white (it used to fade an identically-coloured plate, i.e. invisibly), the desktop canvas mask had to become `none`, and the desktop wrap width needed an `svh` floor because the drawn subject is sized by wrap *height* and a flat 38% clipped his left side at tablet widths. Contrast computed, not eyeballed: the shader is mathematically greyscale (its `u_hue`/`u_saturation` are no-ops on a neutral palette), and a diagonal paper scrim puts the headline at 8.7:1 worst-case. Cursor parallax in the supplied recipe left off on purpose (`ponytail:` comment names the one-line re-enable) — it would add a capturing window `scroll` listener on the site's only pinned, frame-scrubbed section. Evidence: `task-4.2b-hero-keyed-backdrop.md`, `references/task-4.2b/`, `e2e/hero-keyed-backdrop.spec.ts` (4 tests, two of them asserting what Task 4.2 could not — that the drawn pixels actually change under scrub). **Follow-up, same day:** the "wrangler dev dies under 233 concurrent frame requests" blocker recorded above is fixed at the root — `loadSequence()` in `heroSequence.ts` now caps concurrent frame loads at 12 (was: fire all 193/233 at once) instead of only being worked around in tests. Verified by direct reproduction against a live `wrangler dev`, isolated from the browser: the old fire-all pattern killed it in 2/2 attempts (58/233 and 60/233 succeeded, rest `ECONNRESET`); the capped pool succeeded 233/233 in 3/3 attempts, ~1–2.8s, server still serving `/` and `/link` afterward. Full-suite runs against the real `wrangler dev` `webServer` still died mid-run twice more this session, but under a *different* signature (its `InspectorProxyWorker` timing out, not request volume) and only while this session's machine was simultaneously running another agent's Playwright suite — up to 9 node/chromium/workerd processes competing for 4 logical CPUs, 65%+ steady-state CPU measured independent of this suite. Once wrangler dies mid-suite, `reuseExistingServer` doesn't restart it, so everything after fails with `ERR_CONNECTION_REFUSED` regardless of what it tests — not a correctness signal. **Net effect:** the concurrency fix is proven and shipped; a clean 36/36 against real `wrangler dev` needs a run on an uncontended machine, which this session did not have available. `dist/client/hero-frames/` also dropped 10.1 MB → 7.6 MB in passing — the superseded opaque desktop AVIF set was still being copied into every build after the WebP set replaced it.
- [x] Task 4.3 amendment — Music section shader backdrop (human-directed 2026-08-08). Brings the same bold, animated-shader design energy just approved for `/link` (Task 3.6) to the Music section: the mobile-only static hero-image background is replaced by a section-scoped WebGL canvas at every breakpoint, running a human-supplied "moss" Perlin Noise palette. `src/lib/motion/shaderField.ts` is a new shader-agnostic engine extracted from `PlasmaBackdrop.astro`'s former inline script (compile/draw/resize/hidden-tab-pause/reduced-motion, confirmed byte-identical shader+uniforms for `/link` via `git diff` and a programmatic diff) so `/link`'s Plasma backdrop and Music's new one share 100% of the WebGL plumbing; `src/lib/motion/shaders/{plasma,moss}.ts` hold each shader's own source/uniforms, `src/lib/motion/musicBackdrop.ts` adds the `IntersectionObserver`-driven offscreen pause `/link`'s fixed backdrop never needed. Ported the human's supplied React/Paper-Shaders component to this project's vanilla-WebGL pattern rather than adding React/Tailwind/shadcn, since public routes ship zero client React. Because moss is light/cream-dominant (unlike Plasma's dark palette), Music's whole colour scheme flipped from light-desktop/dark-mobile to dark-ink-on-light-field throughout — `--focus-ring` now applies at every breakpoint, every glass card flipped from white- to dark-tinted, and every bare-backdrop text element was re-measured for contrast against the shader's own darkest documented pixel (5 pairs, worst case 3.34:1 against a 3.0 floor for a disabled affordance, everything else ≥5.41:1 against a 4.5 floor). `npm run check` (0/0/0, 3/3 tests) and `npm run build` pass; Music's marginal cost on `/` is ~4.3 KiB gzip. Independent review (`agent-skills:code-reviewer`) found and fixed two real issues: the new e2e suite's `IntersectionObserver` stub globally overwrote a test hook regardless of target, when `/` also runs Hero's own observer (fixed by scoping to `data-music-shader`); and `dispose()` never freed the compiled WebGL program/buffer, a leak on Music's bfcache reinit path that `/link`'s fixed mount (which never calls `dispose()`) never exercised. Evidence: `task-4.3-music-backdrop.md`, `e2e/task-4.3-music-backdrop.spec.ts` (5 tests). Full combined regression with `/link`'s suite: **10/10 passing**. Executed alongside a separate, concurrent session actively reworking Hero in the same working tree the whole time — `Hero.astro`, `HeroBackdrop.astro`, `HeroSocialNav.astro`, `heroSequence.ts`, and `index.astro` were never touched by this amendment. Deliberately NOT built here, per the human's explicit direction: the Hero→Music transition/seam (Task 4.3's own remaining scope) and the scroll-triggered reveal timelines Task 4.3 originally describes — this amendment is the backdrop only.
- [ ] Task 4.3 amendment — Hero opening auto-handoff (human-directed 2026-08-09, implemented and awaiting visual approval). After the first actual Hero canvas paint, a fresh top-of-page visit pauses for 1.2s then drives the existing 2.4s CSS-sticky Hero→Music runway through the sole Lenis instance. It is consumed once per browser session; any pointer, wheel, touch, key, click, hidden-tab event, deep link, restored scroll position, no-JavaScript, or reduced-motion path leaves control to the visitor. Returning manually to the top rewinds the Hero through its existing scroll-driven timeline without restarting the handoff. The loader now holds the intended frame 0 until it settles, only falling forward to a nearest decoded frame when frame 0 fails. Evidence: `task-4.3-hero-autohandoff.md`, `e2e/task-4.3-hero-autohandoff.spec.ts` (7 built-Worker assertions), plus the original Hero scrub/failure/bfcache cases re-run individually after the known long-process Wrangler proxy reset. Independent review found no blocking implementation issue; its coverage recommendations (mobile, touch, bfcache, reset) were added. No production deployment or route/DNS change.
- [x] Task 4.2d amendment — hero: plate restored, one shoot at both breakpoints, scroll-driven push into Music (human-directed 2026-08-09, pending human visual approval). Five pieces of direct feedback on the Task 4.2c build, refined through one round of four questions (all answered with the recommended option): kill the cutout by keeping the artist's own background and fading it out; make the scroll smoother; make the Hero→Music handoff a push; refactor mobile, which "seems to be two hero in one"; and stop the hero going black on the way back up. **The cutout is gone by inverting 4.2b/c's approach entirely** — the frames ship opaque again, plate and all (recovered from git `847059b`, before 4.2b keyed and deleted them), and the plate is *dissolved* rather than cut: `.hero__plate` is a `#BBBBBB` field (the shoot's own measured background colour) sitting behind the canvas and extending past the footage, and the canvas is masked to fade out over its own plate margin, so the composite reads plate → plate → shader with no silhouette edge anywhere. This is why 4.2c could not fix it by feathering: cropping to the subject's union box + 4px left no plate to dissolve. Every crop now deliberately keeps a wide plate margin, and `paintFrame()` sizes the canvas to the drawn frame's exact box so those percentage masks track the footage rather than the viewport. **Both breakpoints now play the same footage** — desktop `864×1080 @ x=1056`, mobile `680×1080 @ x=1240`, both crops of the same 1920×1080 master — so the separate 193-frame mobile shoot (a different room, a different background, unkeyable) is deleted; that second shoot was the "two heroes". 117 frames each (every 2nd): the pair costs the same ~440 MB of held-decoded bitmap the old tight-cropped 233-frame set used, spent on picture area instead of on frames that never got drawn, and `public/hero-frames/` drops 8.0 MB → 2.6 MB. **The push is CSS, not GSAP** — `.hero-scene` is a tall block, `.hero` is `position: sticky`, and Music carries `margin-top: -100svh`, so 100svh hero + 120svh scrub + 100svh push = a 320svh scene in which Music finishes covering exactly as the hero unsticks (verified on the built page: desktop 2880/1980 at 900px, mobile 2700/1857 at 844px). Both are gated on `[data-motion-lifecycle="active"]`, so no-JS and reduced motion collapse to an ordinary stack. **That structure is also the fix for the reported black-on-return**: 4.2c's `advanceToMusic()` faded `#hero` to `opacity: 0` and nothing ever restored it, over a `#0a0a0a` body — nothing is a fired animation now, so there is no state left to restore. Smoothness came from distance, not smoothing: `+=45%` → `SCRUB_VH = 1.2` turns one frame per 1.7px (most of them skipped on any real gesture) into ~9px per frame, with `SCRUB` 0.35 → 0.6; frame index verified exact against scroll at nine probes, and exact on the way back down. Added `--push`, a scroll-linked 0..1 that recedes the footage and lockup (never the plate or shader, so a partly-pushed hero is never a hole onto black) — it exists to fix a specific ugly frame, Music's soft edge sweeping through the artist's face on mobile and reading as him sinking into the moss field. The seam replaces `.music__seam`'s stub with a front-loaded mask on `.music__backdrop`; a first pass at a plain 13svh linear band was a window rather than an edge and double-exposed his face. Deleted in passing: `pauseMotion()`/`resumeMotion()` and `activeLenis` (added for 4.2's mobile lock, which 4.2c removed; nothing has imported them since), `ScrollToPlugin`, and `FrameSet`'s crop/fit bookkeeping. **A diagnosis worth not repeating:** mid-implementation the scrub appeared stuck short of its last frame; instrumenting the drawn index showed it tracking *load progress*, not scroll. Neither a scrub nor a server bug — the server serves 30 frames in 1.7s, and with a real GPU all 117 land in 8.9s and the mapping is exact. Headless has no GPU, `/` runs two SwiftShader contexts, and that starves the AVIF decodes; do not read a headless screenshot of this hero as evidence. Evidence: `task-4.2d-hero-plate-and-push.md`, `e2e/task-4.2-hero-sequence.spec.ts` (10 tests, rewritten — `.pin-spacer` no longer exists, and one test is a direct regression test for the black-on-return bug), `e2e/hero-plate-backdrop.spec.ts` (7 tests, replacing `hero-keyed-backdrop.spec.ts` — its central assertion inverts from ">25% transparent" to ">99% opaque"). One pre-existing flake stabilised: `task-4.3-music-backdrop.spec.ts`'s pause/resume test polled for a shader draw on a 5s default, but that loop manages 2–3 draws per 4s without a GPU — it fails at `HEAD` too, and is confirmed not caused by the new seam mask (identical draw counts masked and unmasked). Note for whoever reads it: Task 4.2c shipped without ever getting a `todo.md` entry; its record is `task-4.2c-hero-restyle.md` only.
- [x] Task 4.2e amendment — Hero backdrop posterisation reduction (human-directed 2026-08-09, pending the same Hero visual approval). `HeroBackdrop.astro`'s `u_intensity` changed from `0.35` to `0.78`, so its existing `2 + floor(u_intensity * 18)` quantization rises from 8 to 16 tonal levels. This halves the broad band discontinuity without adding code, bytes, rendering passes, listeners, or animation work; the intentional stepped field remains. Evidence: `task-4.2e-hero-backdrop-banding.md`; `npm run check`, `npm run build`, all 7 `hero-plate-backdrop` Playwright assertions, and direct built-Worker Playwright captures at desktop and mobile pass.
- [x] Task 4.2f Ponytail amendment — remove the Hero backdrop's unreachable cursor shader path (2026-08-09, pending the same Hero visual approval). Cursor `presence` was hard-coded to zero and had no event wiring, so the unused uniforms, pointer packing, macros, and per-fragment branches were deleted rather than shipped dormant. `HeroBackdrop.astro` drops 2,876 UTF-8 source bytes (19,716 → 16,840) with all active field values unchanged; stale keyed/mobile comments were corrected in the same scoped cleanup. Evidence: `task-4.2f-hero-backdrop-dead-cursor-removal.md`; `npm run check`, `npm run build`, emitted-bundle audit, direct Playwright Chromium WebGL compile/link pass, and fresh built-Worker desktop/mobile behavior checks. The final shared-process Worker result is the **7/7** Task 4.2g suite pass recorded in `task-4.2g-hero-worker-verification-stability.md`.
- [x] Task 4.2g Ponytail verification amendment — stabilize the shared built-Worker Hero suite (2026-08-09, pending the same Hero visual approval). The shader-only breakpoint test previously triggered two irrelevant 117-frame Hero loads even though it asserts only the independent backdrop canvas; it now aborts those 234 requests while the sequence remains covered by its dedicated opacity/scrub/frame tests. Playwright pins local Worker/inspector endpoints to avoid the host's random-proxy startup issue. Evidence: `task-4.2g-hero-worker-verification-stability.md`; `npm run check`, `npm run build`, `git diff --check`, and the normal `hero-plate-backdrop` suite pass **7/7 in one shared built-Worker process**.
- [ ] Task 4.3 amendment — Hero opening auto-intro (human-directed 2026-08-17, implemented and human-approved). Every ordinary page load pauses after first paint, then traverses the existing 2.4s/120svh Hero frame scrub through the sole Lenis instance and stops on frame 117. Music remains one viewport below; the visitor owns the following Hero→Music push. Frame 0 and frame 117 load first, normal reloads replay, and bfcache/deep-link/reduced-motion/no-JavaScript/input safeguards remain. Evidence: `task-4.3-hero-autohandoff.md`, `e2e/task-4.3-hero-autohandoff.spec.ts` (7 assertions).
- [ ] Task 4.3 subsection — Music timeline (implemented and human-approved 2026-08-17). Ports the legacy 120% desktop pin/reveal and unpinned mobile entrance through the existing motion lifecycle, without changing the approved moss backdrop or Hero seam. Evidence: `task-4.3-music-timeline.md`, `e2e/task-4.3-music-timeline.spec.ts` (4 assertions), `e2e/task-4.3-music-backdrop.spec.ts` (5 assertions), and responsive captures.
- [x] Task 4.3 subsection — Shows timeline (implemented and human-approved 2026-08-17; Signal Board amendment user-directed 2026-08-22). Ports the legacy 250% desktop pin, title/panel reveal, photo parallax and tilt, plus row idle/hover behavior through the existing lifecycle. The Signal Board keeps the user-approved Vercel five-row fallback until real Task 5.2 records take precedence. Evidence: `task-4.3-shows-timeline.md`, `task-3.2-shows-signal-board.md`, `e2e/task-4.3-shows-timeline.spec.ts` (5 assertions), and responsive captures.
- [x] Task 4.3 subsection — Footer timeline (implemented and human-approved 2026-08-17). Ports the legacy desktop booking/link stagger and mobile content entrance through the existing lifecycle, with no pin or Task 4.4 wave changes. The mobile end position resolves at the natural document end, since legacy's endpoint was unreachable in the final Footer. Evidence: `task-4.3-footer-timeline.md`, `e2e/task-4.3-footer-timeline.spec.ts` (5 assertions), and responsive captures.
- [x] Task 4.3: Port section timelines and transition seams one at a time. Hero, Music, Shows, and Footer are human-approved; the 14 combined timeline checks and 5 Music-backdrop regressions pass. Evidence: the Task 4.3 subsection records and `e2e/task-4.3-{music,shows,footer}-timeline.spec.ts`.
- [x] Task 4.4: Port waves and shader; avoid public React unless approved. Human-approved 2026-08-17: `footerWaves.ts` ports the legacy simplex field to native canvas with pointer inertia, offscreen/hidden-tab pause, and a frozen reduced-motion render; no React or dependency added. Evidence: `task-4.4-footer-waves.md`, `e2e/task-4.4-footer-waves.spec.ts` (5 assertions), and responsive captures.
- [x] Task 3.2 amendment — Shows Signal Board (user-directed 2026-08-22). Supersedes the earlier content-honest empty state: restores the user-approved five-row legacy Vercel fallback and presents it as a date-led Signal Board. Real Task 5.2 records automatically take precedence. Evidence: `task-3.2-shows-signal-board.md`, Shows browser suite 5/5, and responsive captures.
- [ ] Task 4.5: Complete reduced-motion, keyboard, focus, and failure-mode QA.

### Checkpoint 4: Motion and accessibility parity approved

## Phase 5 — Content cutover

- [ ] Task 5.1: Implement the production Keystatic schema and auth configuration.
- [ ] Task 5.2: Migrate and validate real shows, releases, links, copy, and artwork.
- [ ] Task 5.3: Complete non-developer editorial UAT from edit through rollback.
- [ ] Task 5.4: Write the Keystatic client guide.

### Checkpoint 5: Client editorial UAT approved

## Phase 6 — Cleanup and release candidate

- [ ] Task 6.1: Remove superseded Next/Tailwind/Supabase runtime code and dead dependencies.
- [ ] Task 6.2: Optimize the approved media and font pipelines.
- [ ] Task 6.3: Add CSP, security/cache headers, SEO files, CMS no-index, and error behavior.
- [ ] Task 6.4: Pass browser, device, accessibility, content, CMS, build, and performance matrices.

### Checkpoint 6: Release candidate approved

## Phase 7 — Production cutover

- [ ] Task 7.1: Configure the client-owned Cloudflare production project and domain readiness.
- [ ] Task 7.2: Perform the controlled DNS/content cutover and verify rollback.
- [ ] Task 7.3: Complete the 7–14 day observation window and explicitly approve old-infrastructure retirement.

### Checkpoint 7: Migration complete
